apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-data
  namespace: lab2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # For local development, you may need to adjust storageClassName
  # storageClassName: standard
